# Backend API URL
REACT_APP_API_URL=http://localhost:8000/api

# Admin authentication key (must match backend ADMIN_KEY)
REACT_APP_ADMIN_KEY=your_admin_key_here

# Public URL / Base path for deployment (use "/" for root, "/admin" for subdirectory)
PUBLIC_URL=/admin
